import type { Metadata } from "next";

export const metadata: Metadata = { title: "Set Your Password — Dayflow" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
