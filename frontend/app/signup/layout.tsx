import type { Metadata } from "next";

export const metadata: Metadata = { title: "Create Workspace — Dayflow" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
